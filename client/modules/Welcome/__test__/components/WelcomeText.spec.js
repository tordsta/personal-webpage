/*
import React from 'react';
import test from 'ava';
import { shallow } from 'enzyme';
import WelcomeText from '../../components/WelcomeText';


test('header has class', t => {
  const wrapper = shallow(
    <WelcomeText />
  );

  t.true(wrapper.hasClass('welcome-header'));
});


test('text has class', t => {
  const wrapper = shallow(
    <WelcomeText />
  );

  t.true(wrapper.hasClass('welcome-text'));
});
*/

